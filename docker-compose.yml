services:
  db:
    image: ankane/pgvector
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: rfq_agent
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
